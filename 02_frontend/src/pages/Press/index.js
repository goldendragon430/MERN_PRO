import React from 'react'
import { Press as PressController } from '../../components/main/Press'

export const Press = (props) => {
  return (
    <>
      <PressController {...props} />
    </>
  )
}
