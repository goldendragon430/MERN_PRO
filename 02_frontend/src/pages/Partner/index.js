import React from 'react'
import { Partner as PartnerController } from '../../components/main/Partner'

export const Partner = (props) => {
  return (
    <>
      <PartnerController {...props} />
    </>
  )
}
