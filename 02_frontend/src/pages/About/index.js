import React from 'react'
import { About as AboutController } from '../../components/main/About'

export const About = (props) => {
  return (
    <>
      <AboutController {...props} />
    </>
  )
}
