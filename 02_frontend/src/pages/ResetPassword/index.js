import React from 'react'
import { ResetPassword as ResetPasswordController } from '../../components/main/ResetPassword'

export const ResetPassword = (props) => {
  return (
    <>
      <ResetPasswordController {...props} />
    </>
  )
}
